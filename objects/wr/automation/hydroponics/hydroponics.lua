function init()
    -- message.setHandler("setPlantImage", function (_,_, image)
    --     object.setConfigParameter("animationParts", { plant = image })
    --     animator.setPartTag("plant", "partImage", image or "")
    -- end)
end
